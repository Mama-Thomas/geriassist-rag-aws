version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/app:/app/app
      - ./data:/app/data
      - faiss_data:/app/faiss_index

volumes:
  faiss_data:
